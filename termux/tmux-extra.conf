# Termux-only tmux extras, sourced by ~/.tmux.conf (see tmux/config)

# prefix+u: open the last URL visible in the pane in the external browser
# (requires the Termux:API app)
bind u run-shell "tmux capture-pane -p -S - | grep -oE 'https?://[^ )\">,]+' | tail -1 | xargs -r termux-open-url"
